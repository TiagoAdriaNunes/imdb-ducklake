name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Python quality gates
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install Python
        run: uv python install 3.12

      - name: Install locked dependencies
        run: uv sync --locked --extra app

      - name: Check formatting
        run: uv run ruff format --check .

      - name: Lint
        run: uv run ruff check .

      - name: Lint dbt SQL
        run: uv run sqlfluff lint dbt/models dbt/tests

      - name: Type check
        run: uv run mypy src

      - name: Parse dbt project
        run: uv run dbt parse --project-dir dbt --profiles-dir dbt --no-partial-parse

      - name: Test
        run: uv run pytest -m "not smoke" --cov=imdb_ducklake --cov-fail-under=85

      - name: Build package
        run: uv build
